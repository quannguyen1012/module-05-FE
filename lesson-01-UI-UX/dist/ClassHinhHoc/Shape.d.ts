export declare class Shape {
    private _x;
    private _y;
    constructor(x: number, y: number);
    get y(): number;
    get x(): number;
    set y(y: number);
    set x(x: number);
    toString(): string;
}
